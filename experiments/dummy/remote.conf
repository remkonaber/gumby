
# TODO: Update it so it reserves a few nodes to run sleep 10 in them
experiment_name = "Dummy_remote"

workspace_dir = './'

#head_nodes = 'emilon@fs3.das4.tudelft.nl',
head_nodes = '208',

tracker_cmd = 'sleep 20'

local_setup_cmd = 'echo local_setup_cmd'
remote_setup_cmd = 'echo remote_setup_cmd'

local_instance_cmd = 'dummy_logging_test.sh'
remote_instance_cmd = 'dummy_logging_test.sh'

post_process_cmd = 'echo post_process_cmd'
